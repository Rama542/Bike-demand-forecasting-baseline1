{
  "name": "veloai-deployment",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && cd .. && rm -rf public && cp -r frontend/dist public"
  }
}
